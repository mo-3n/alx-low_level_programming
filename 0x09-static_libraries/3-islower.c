#include "main.h"

/**
 * _islower - This function do something.
 *
 * @c: Argument description.
 *
 * Return: return value description.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
