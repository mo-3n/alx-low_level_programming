#include "main.h"

/**
 * _islower - This function do something.
 *
 * This function perform an action,
 * end of description.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
