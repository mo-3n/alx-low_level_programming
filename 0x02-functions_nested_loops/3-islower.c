#include "main.h"

/**
 * _islower - This function do something.
 *
 * @c: This function perform an action,
 *
 * Return: end of description.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
