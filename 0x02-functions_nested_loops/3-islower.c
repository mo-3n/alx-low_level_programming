#include "main.h"

/**
 * _islower - This function do something.
 *
 * This function take an int c and perform an action,
 * and return 1 or 0.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
