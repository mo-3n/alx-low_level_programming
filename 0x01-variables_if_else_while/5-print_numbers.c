#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numbr = 0;
	while (numbr < 10)
	{
		printf("%d", numbr);
		numbr++;
	}
	printf("\n");
	return (0);
}
